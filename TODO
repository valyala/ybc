- More tests.
- Documentation.
- Ports to other platforms (Windows, MacOS).
- Killer app.
- API bindings for popular programming languages (Java, Python, PHP, C#, Lua).
- Data corruption detection.
- Optimistic concurrency control.

Architecture enhancements:
- Improve m_storage_allocate() algorithm from O(n*n) to O(n*ln(n)).
- Split single m_de->pending_items list into multiple lists (aka hash buckets)
  in order to increase m_de_get_item() speed when there are a lot of pending
  items.
